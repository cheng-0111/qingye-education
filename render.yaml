services:
  - type: web
    name: qingye-ai
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: DEEPSEEK_API_KEY
        sync: false
